/**
 * environment.ts
 * --------------
 * Dev environment values.
 *
 * Angular replaces this file in production builds (see angular.json).
 */

export const environment = {
    production: false,

    // Express API base URL
    apiBaseUrl: "http://localhost:3002/api",
};
